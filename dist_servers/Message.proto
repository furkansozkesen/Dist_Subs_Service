syntax = "proto3";

message Message {
    enum Demand {
        STRT = 0;
        CPCTY = 1;
    }

    Demand demand = 1;
    Response response = 2;

    enum Response {
        YEP = 0;
        NOP = 1;
    }
}
