syntax = "proto3";

package client_protos;

message AppConfig {
    int32 faultTolerance = 1;

    enum ActionType {
        START = 0;
        STOP = 1;
    }
    ActionType action = 2;
}
